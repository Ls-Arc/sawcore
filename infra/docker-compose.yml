services:
  web:
    image: oven/bun:1.3.13
    working_dir: /workspace
    volumes:
      - ..:/workspace
    environment:
      NODE_ENV: development
      PORT: 3000
    command: bun run web:start
    ports:
      - "${HOST_PORT:-3000}:3000"
